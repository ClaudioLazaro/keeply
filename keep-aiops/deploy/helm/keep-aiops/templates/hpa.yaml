{{- if .Values.aiopsApi.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "keep-aiops.apiName" . }}
  labels:
    {{- include "keep-aiops.labels" . | nindent 4 }}
    {{- include "keep-aiops.apiSelectorLabels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "keep-aiops.apiName" . }}
  minReplicas: {{ .Values.aiopsApi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.aiopsApi.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.aiopsApi.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
